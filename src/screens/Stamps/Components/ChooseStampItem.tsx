import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ChooseStampItem = () => {
  return (
    <View>
      <Text>ChooseStampItem</Text>
    </View>
  );
};

export default ChooseStampItem;

const styles = StyleSheet.create({});
